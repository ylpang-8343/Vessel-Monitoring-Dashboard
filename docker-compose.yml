services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: vessel
      POSTGRES_PASSWORD: vessel
      POSTGRES_DB: vessel_monitoring
    ports:
      - "5432:5432"
    volumes:
      - vessel_pg_data:/var/lib/postgresql/data

volumes:
  vessel_pg_data:
